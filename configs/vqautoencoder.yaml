model:
  base_learning_rate: 4.5e-6
  target: ldm.models.autoencoder.VQModel
  params:
    monitor: "val/rec_loss"
    embed_dim: 8
    n_embed: 16384
    ddconfig:
      double_z: False
      z_channels: 8
      resolution: 256
      in_channels: 3
      out_ch: 3
      ch: 128
      ch_mult: [1,2,4,4] #num_down=len(ch_mult)-1
      num_res_blocks: 2
      attn_resolutions: [16]
      dropout: 0.0
      num_groups: 32
    lossconfig:
      target: ldm.modules.losses.vqperceptual.VQLPIPSWithDiscriminator
      params:
        disc_conditional: false
        disc_in_channels: 3
        disc_start: 50001
        disc_weight: 0.75
        disc_num_layers: 2
        codebook_weight: 1.0
        n_classes: 16384
    

data:
  target: main.DataModuleFromConfig
  params:
    batch_size: 1
    num_workers: 0
    wrap: False
    train:
      target: data.data_loader_online2.dataloader
      params:
        cfg:
          version: advanced_12Hz_trainval
          dataroot: /remote-home/share/nuScenes/samples/CAM_FRONT/
          device: 6
        split_name: train
    validation:
      target: data.data_loader_online2.dataloader
      params:
        cfg:
          version: advanced_12Hz_trainval
          dataroot: /remote-home/share/nuScenes/samples/CAM_FRONT/
          device: 6
        split_name: val


lightning:
  callbacks:
    image_logger:
      target: main.ImageLogger
      params:
        batch_frequency: 5000
        max_images: 8
        increase_log_steps: False
    

  trainer:
    benchmark: True
    gpus: 0,1,2,4,5,6

    